import { opt } from 'src/lib/options';

export default {
    icon: opt('󰸗'),
    showIcon: opt(true),
    showTime: opt(true),
    format: opt('%a %b %d  %I:%M:%S %p'),
    rightClick: opt(''),
    middleClick: opt(''),
    scrollUp: opt(''),
    scrollDown: opt(''),
};
