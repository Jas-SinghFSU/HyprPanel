import { opt } from 'src/lib/options';
import { primaryColors } from '../../../colors';

export default {
    size: opt('0.13em'),
    radius: opt('0.7em'),
    color: opt(primaryColors.surface0),
};
